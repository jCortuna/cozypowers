---
description: Shape a feature idea into a structured spec before planning begins
---

Use the **shaping-specs** skill from the cozypowers plugin.

Take the feature I describe (or the one we're already discussing) and shape it into a spec: read any `standards/` and `product/` context in this project first, ask me the shaping questions, then draft a spec folder under `specs/` — plan, shape, standards, and references — and summarize it for my approval before writing anything to disk. Do not write implementation code during this command, and do not auto-invoke `/plan`.
